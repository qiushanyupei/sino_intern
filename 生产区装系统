首先是对于固件版本的确认和升级
看软硬件配套表SFC79xx确定BSP、kernel、uboot版本
登录
1、root
Net@st&l

2、reboot查看BSP软件版本（看BL2）
查看kernel版本
uname -a 
查看uboot版本
fw_printenv | grep version
	
3、配置IP(重启后需要重新配置)
ifconfig eth1 192.168.1.200（配置设备的网卡）ipconfig只是暂时设置的命令
然后控制面板配置本身的网卡为192.168.1.30，使内网相连，需要关闭防火墙

4、上传BSP文件_Ls1043（先从不知道什么地方下载），传到/tmp文件夹下
	使用WINSCP软件用SFTP传输协议，默认端口22

5、给更新文件设置执行权限
chmod +x update_1043_flash.sh（里面有升级kernel、BSP、uboot的所有命令，所以用它）
	然后执行
./update_1043_flash.sh

6、开始装系统前置
下载ubuntu18.04版本相关系统压缩包
根据7132C软硬件套件表下载对应软件（如网元软件版本号、随便来一个，如果没特殊要求），放到user-app目录下

7、启动脚本：res->win->ntp-server.bat
主目录下：start-server_win.bat

在可交互界面逐渐输入要连接的ip和要安装的软件包
板卡型号：看标签输入（如SFC7132C）
hostname设置为：Sino-Telecom
复制命令到串口命令行执行
按q键退出

装好系统后
账号：super
密码：123456

先设置ip才能设置mac地址，左侧标签是底板版本（左上PN号，左下SN号，右侧mac地址），右侧标签是CPU版本

装错版本后重装系统：
先reboot
然后空格打断重启（hit any to stop autoboot）
env default -a 
saveenv
reset 
再回车就会重启
cd /app/
rm -rf *
cd /flash/
rm -rf *
然后再reboot
登录后重新
ifconfig eth1 192.168.1.200
然后再跑脚本装系统
