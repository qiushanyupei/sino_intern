从默认系统返回安装的openeuler系统：/app/opt/rootfs/fstool.sh setenv

配临时ip，要求没有ip冲突，然后配网关，要可以ping通自己电脑的ip
ifconfig eth2 192.168.27.206（对于7948HQ来说eth2是对外网口）
route add default gw 192.168.27.1
研发区装系统，在系统确定的情况下需要换特定软件到user-app目录下
然后启动start_win.bat
板卡名称写实际
host(自己厂家就写sino):Sino-Telecom
安装好后reboot
可以解压再用Update.sh安装或者直接下载脚本安装(也要放在app/version下)
cd /app/version
然后解压tar -zxvf 对应文件名(似乎可以不解压)
./packages/update.sh 软件文件名（再升级软件）
sync(把缓存写入硬盘中)
最后reboot

单独更新的脚本对应update.sh
整框更新的脚本对应update_all.sh

或者升级系统的统一脚本（整框升级包），如果直接执行，会对整框的所有槽道升级，可以像下面一样指定槽道号，似乎是会对本槽道和指定槽道做升级
xxx.sh 1-3
xxx.sh 23
xxx.sh all(即在主控上给其他槽道升级，但是需要对业务卡重启(复位解复位))
整框升级软件，主控上给业务卡复位后，重启，主控会自动解复位业务卡
在主控对板卡进行命令复位重启：在主控进入root用户->输入slot.sh命令->slot.sh stop 7->slot.sh start 7
