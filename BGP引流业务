BGP路由策略校验：
1、入方向策略校验：是否过滤掉不该接收的路由、是否正确修改BGP路由属性
2、出方向策略校验：是否只通告允许的网段，没有把内部路由错误对外发布；发给不同邻居的策略是否区分开；AS‑PATH、community、MED 修改是否符合规划
3、语法逻辑错误
4、是否出现环路

BFS = BGP‑Flowspec‑Steering(BGP规定流量引导)
BFS镜像：复制报文，原始报文继续原有转发，复制份送给安全设备
BFS 牵引（Redirect，重定向引流）：改写报文转发路径，原始报文不再走原路径，全部送到安全资源池，清洗完成后回注
