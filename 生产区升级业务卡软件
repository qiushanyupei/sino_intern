sudo su
上传4759软件包到该目录下（/app/version）
cd /app/version
chmod 777 *
./dpi_oem_dev_4759-ctc-aarch64.dfo.sh
reboot
sudo su
安装软件版本
cd /app/version
cd tap_YH_DPI-1.0.26-FULL
./install_mini.sh --install
reboot


验证扣卡是否能ping通：(软件问题)（1、2、3、4四个ip）
np.local stop
np.local start
重启一下，等一会，再看眼是否能ping通

修改主控日期，预期业务板日期能同步
date -s "2026-05-29 16:06:30"
hwclock -w
查看进程有无起来（日期和进程都要等一段时间）
netstat -apn |grep 4222 |grep 10.96.70

执行补丁命令
cd /app/version
rpm -ihv tap_tools_yh.rpm --force
reboot


主控每次重启要执行命令
ifconfig eth1 192.168.1.200/24
systemctl restart sfc.service
systemctl restart tap_master
systemctl restart redis
killall led.d
