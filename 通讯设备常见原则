同一类业务规则，多个用户之间流量隔离、互不抢占，如端口转发型规则。
用户 1：0/F/1 ↔ 0/F/2
用户 2：0/F/3 ↔ 0/F/4
流量不能混叠、资源互相隔离

不同业务类型的规则，流量互不隔离，可以叠加共用带宽 / 资源。
类型不同 = 业务不一样、规则用途不一样
例：
类型 1：端口转发（流量透传）
类型 2：ACL 流量过滤
类型 3：组播复制
类型 4：镜像抓流
规则通道 / 端口资源允许复用、流量可以叠加
同一个物理端口，可以同时承载【转发规则流量】+【镜像流量】；
不同业务类型互不排斥，不需要互相预留独立资源；
