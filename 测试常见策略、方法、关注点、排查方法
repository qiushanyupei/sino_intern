光模块测试流程：
需要通过接Bypass设备和分流汇聚设备
测试灵敏度：光模块能否正常接收经过 Bypass 设备内部光开关和跳线后，功率略有衰减的信号。
测试响应速度：Bypass 设备在心跳检测失败而触发 Bypass 模式切换时，光模块能否正确处理链路中断/恢复的瞬态过程，会不会因此“卡死”或产生错误告警。

打流测试流程：
1、串口对于新版卡设置ip，装系统、通过脚本设置一些参数、打流（一般情况下打流/多次热插拔情况下打流（通过小米电源控制循环上下电多次））、写打流报告（（包含端口统计信息截图、系统信息截图（包含唯一的SN号）（show system info）、仪表软件统计信息截图）
），需要保证0丢包
2、配置打流转发规则需要通过每条Tx、Rx，到1口可以通过内部回环或外部回环返回
3）对于7408这种小型机框，边缘槽道号由于布线原因，光功率损耗打，对于打流板卡需要到该槽道号进行单独测试
4）必须要仔细看仪表和分流设备收发包数是否一致，避免漏掉仪表问题和分流设备误码问题

常见问题和排查方法：
通过show port statistics查看软件是否起来
1）端口不up排查：没设置端口速率；换光模块；仪表的fec和分流设备端口的fec不一致，需要设置。如使用port xxx fec rs
2）改ip后无法ping通排查方案：首先进入root权限ping下网关和本身ip,ethtool 网卡名 可以查看是否成功连接
     如果ping不同网关可能是网线问题，换一根网线
3）如果单个口出现丢包，可能是光模块问题，换好的光模块解决（show equipment可以看卡有没有起来，recognizing表示还在识别，operational说明已经起来了，可能7408线卡需要一定时间载入）show equipment内可能是缓存信息，可以通过no equipment 6 card清除缓存，然后show equipment重新读取设备信息
4）配置ip后ssh连接卡顿，可能是ip冲突（多个网卡指定单个ip）
5)仪表软件显示收发数据数量不同，可能是软件延迟或光模块接触不良（重新插拔解决）
光模块up后无流排查顺序：路由规则->实际光模块连接；打流丢包排查顺序：光模块->光纤（如果是连接仪表的口丢包，可以尝试更换仪表口，10G仪表容易出现问题）
send disconnect:Broken pipe意味着路由规则无法满足转发流向，如果之前已配好，可能是设备重启清除了规则；uptime命令按顺序展示当前时间、系统已运行时间
6)lspci命令为list Peripheral Component Interconnect(外围组件互联）,了解和其相关所有设备的信息
7）主控卡上show equipment无法查看其他插槽上的板卡问题，生产时有个电阻搞错
8）对于GE的模块速率要设置为1G
9)tail -f -n 400 /app/var/log/startup.log查看设备启动日志
10)sudo tail -f /app/var/log/debug.log 查看debug日志

梯度逼近测试（为了性能拐点）：对于汇聚分流设备要逐渐减小包长，测试pps极限（小包特性是高频率中断，每一个小包都意味着交换芯片需要做一次查表（MAC地址表）、一次转发决策、一次CRC校验。）
线速（Line Rate） 是指网络设备在某个端口上，能够以该端口支持的最高理论速率，无任何丢包地转发最小字节数据包的能力。
找到它在不丢包情况下的最大PPS值
测试阶段	包长	包转发率(PPS)	压力水平                        	目的
1. 基线测试	1518字节 (大包)	很低 (~81k pps@千兆)	轻松	验证基本连通性和无丢包
2. 混合测试	IMIX (混合包长)	中等	正常	              模拟真实互联网流量 (通常能过)
3. 压力测试	256字节	较高	明显压力	                    开始暴露查表延迟            （一般要做24小时连续打流测试）
4. 极限测试	64字节 (最小)	最高 (1.488M pps@千兆)	极限	唯一标准：看是否丢包

测试方法：
1、IP过滤
2、VLAN（虚拟局域网）过滤：单层/双层VLAN（QinQ）；VLAN范围：支持匹配 VLAN 100-200 的范围吗；剥离/保留VLAN标识
3、1口连接仪表，然后将流按顺序遍历后续2到32接口，最终回到1口,看流是否通畅（线速回环打流测试）

关注点：
1、以太网帧Mac地址、VLAN\IP、端口号
2、背板带宽
3、上电稳定性，用脚本判断有没有什么进程，然后不断重启，测试稳定性。
4、测不同端口衰减，要做到0误码；不同端口到交换芯片距离不同，但是都要0衰减

打流测试形式：
1、端到端性能测试 (End-to-End)：拓扑：测试仪 Port1 → DUT 入口 → DUT 出口 → 测试仪 Port2（数据包从端口进入，在DUT中被处理，再从端口出）
2、全网状测试 (Full Mesh)：用于测试设备背板带宽或分布式系统的交换能力，避免单端口成为瓶颈。
3、一对一（反射/回环）测试
4、模拟真实网络中的多流混合、优先级、过滤规则
5、突发与过载测试 (Burst & Overload)；如背靠背：以合法的最小帧间隔，向网络设备连续发送一定数量的数据帧。（时间线上是一个紧接一个，没有任何空隙）
6、长稳测试 (Soak / Endurance)：模拟真实网络长期运行
7、有状态流量测试 (Stateful)：区别于普通 UDP 无状态测试，模拟 TCP 会话。

针对NP模组的一般测试方法
1、插入模组
2、远程连接查看信息
3、如果CPLD版本和软件版本不对要重新刷（升级BIOS）
4、给模组卡新装系统（有SOP）
5、配置
6、测端口、网口、打流、测试
