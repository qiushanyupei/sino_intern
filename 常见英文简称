psu: power supply unit(电源)
ARP：Address Resolution Protocal
DUT: Device Under Test
SC:Shelf Controller （机框控制器，即主控卡）
LOS:loss of signal
AutoNeg(Auto-Negotiation):自动协商是一种机制，允许两个相连的端口互相通告自己支持的能力，并自动选择双方都支持的最高级工作模式（如速率、双工模式、流控等）
光模块虽然速率固定，但是可以通过协商实现速率下调兼容低速率光模块
profile对于硬件来说可以理解为配置
XG指10G，有罗马数字引申而来
HG指100G，H（hundred）
npi：new product introduction（新产品推出部门），在欣诺叫生产
bsp:board support package(用于让硬件可以被软件驱动，类似于中间件)
SFC ：Service Function Chaining（服务功能链），用于编程指定网络流量的服务顺序（如：用户流量 → 防火墙 → 入侵防御系统(IPS) → 负载均衡器 → 目标服务器）
U-Boot 负责嵌入式设备里的“BIOS + 引导管理器
AverageLen(Tx/Rx)：报文的平均长度
mtu表示Maximum Transmission Unit（最大传输单元），通常以字节为单位
PPS(packet per second)：包长越长，PPS越低
Qos（quality of service）控制数据包的优先级
MA网络————城域网（Metropolitan Area Network，MAN）
SerDes:Serializer / Deserializer(串行器和解串器)
STC (spirent test center)
octet意思是8位字节
Inc Byte : 单位时间内流经某端口/某条流的字节数增量（用于实时带宽计算或流量突发检测）
PCBA：Printed Circuit Board Assembly（安装好的PCB板料号）
rma:Return Material Authorization(可以理解为退货处理)
EEPROM全称：Electrically Erasable Programmable Read-Only Memory（也属于寄存器那块）
MCP = Model Context Protocol（模型上下文协议),是外部工具统一接口，通常用json写，具体代码在MCP服务器上
netif（network interface）（似乎是Linux内置 网卡流量统计器）
TSU（芯片内置温度模块）Thermal Sensor Unit   热传感器单元
hwi（hardware image）将硬件扇区已镜像的方式操作
PHY(physical Layer Transceiver),可以理解为物理层的收发器
PCN（Product Change Notification）产品变更通知
BGA（Ball Grid Array）球栅阵列封装。引脚不是两侧排布，而是芯片底部以矩阵形式排布锡球（焊球）；DDR、FLASH、主控芯片背面大量采用该封装；日常说的植球（Reball），就是重新制作 BGA 底部锡球。
pcs-pieces，也就是指“片”
SAI（Switch Abstraction Interface，交换机抽象接口）
Network Packet Broker（NPB）：流量分发器，好像可以负责流量去重
revision：通常指修订版本
ECPU port	Embedded CPU Port（embedded CPU port）嵌入式CPU端口
CPU Packet Interface Port（CPI port）：CPU 报文接口端口。CPU 与芯片之间的另一种报文接口，通常用于带外管理或特定控制平面报文通道，与 CPU port 的区别在于传输协议 / 队列映射不同。
RC port（Recirculation Port）再循环端口。报文处理到一半需要重新进入流水线（如二次查表、隧道封装后再转发、策略路由重定向）时，通过 RC port 将报文送回芯片输入端重新处理。0~10 共 11 个再循环端口，数量越多表示芯片支持的并行回收处理能力越强。
Resource mode	—	资源模式。single 表示单实例模式（芯片资源作为一个整体使用）；与之相对的是多实例模式（如将一颗芯片虚拟成多台逻辑交换机）。
kg = Kilo-Gate 资源调度模式（云合CLX 系列专有），代表按流 / 会话共享硬件表项、Buffer、Classifier (分类器)资源；
对比其他模式：static（静态划分资源给每个 slice），kg 更适合 TAP、流量采集、动态大流量场景。
TRex（Realistic Traffic Generator）是Cisco 开源、基于 DPDK（专门用于网络数据面报文处理的一个intel开源项目）（Data Plane Development Kit中文：数据平面开发套件） 的高性能软件流量发生器
latency mode = normal：延迟模式
normal：均衡模式，吞吐和延迟折中；
low_latency：低延迟模式（牺牲部分缓存，降低转发时延）；
deep_buffer：大缓存模式，应对突发流量，增加时延。
Die bitmap 0x00000003：Die（裸芯片）掩码，bit0、bit1 置 1，代表 2 个 Die 都启用（双裸片封装）
Bin(也有分类筛选的意思) bitmap 0x00000001：有哪些分类筛选资源组被启用
Plane bitmap 0x00000003：转发平面掩码，bit0/bit1=1，两个转发平面全部启用。
Slave plane bitmap 0x00000000：从转发平面掩码，0 表示没有配置 Slave 平面，全部为主平面运行，非主从备份架构。
FPU（Filter Processing Unit，报文过滤 / 分类处理单元）
ETH macro bitmap 0x00000000 00000FFF：以太网 Macro（PHY/SerDes 硬件宏单元）掩码。
后半段 00000FFF = 低 12bit 全 1：最多 12 个以太网 MAC/SerDes 通道可用；
SPI：Serial Peripheral Interface，串行外设接口，一种高速串行总线。没有专用标准接头！它不是像网口、USB 那种看得见的插座，它就是 PCB 板上的 4 根铜皮走线，芯片引脚之间直接相连。
INTR_NFE_ECC = 3520：中断计数器，NFE（Network Fabric Element，片上网络交换矩阵）ECC（error correcting code） 错误中断
diag 通常指diagnostic的英文缩写
RC = Release Candidate，发布候选版，属于软件版本的一种标记，不是正式发布版
compromise动词：损坏
**LVM**：Logical Volume Manager中文：**逻辑卷管理器**
EMC（Electromagnetic Compatibility，电磁兼容）
NDA = Non‑Disclosure Agreement保密协议
Medium‑density通常指Flash等存储芯片的存储密度
CAN:Controller Area Network，控制器局域网,是一种串行通信总线，专为汽车、工业设备等嵌入式场景设计，用来让板子上多个单片机 / 控制器之间互相收发数据。
ADC ：Analog‑to‑Digital Converter，模数转换器。
timer:定时器是 MCU 内部的硬件外设。
1. **定时**：到设定时间产生中断，实现周期性任务，比如每隔 1ms 执行一次函数
区分：硬件定时器（只依靠硬件电路逻辑）（片上外设，精度高，不占用 CPU 循环等待）；软件定时器（靠 CPU 模拟，精度差）。
**DMIPS**：Dhrystone（整形基准） Millions of Instructions Per Second
1. Single‑cycle multiplication 单周期乘法- 32 位整数乘法，**只需要 1 个 CPU 时钟周期就能出结果**。
# SRAM**英文全称：Static Random‑Access Memory，静态随机存取存储器**，相当于运行内存，只要不断电，不需要刷新，数据就保持住
