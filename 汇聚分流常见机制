通过DPI(Deep Packet Inspection（深度包检测）):不仅看头部，还要深入到报文的数据负载部分（Payload）
基于报文的五元组（源IP、目的IP、源端口、目的端口、协议号）或更丰富的头部字段（如MAC、VLAN、MPLS标签、隧道ID）进行分流。
测试重点：设备能否正确、稳定地从头部提取这些字段？如果头部有选项字段（如IP Option）或扩展头（如IPv6扩展头），设备能否跳过它们正确提取后面的端口号？

哈希均衡：通过对协议头部进行哈希mapping，控制不同流量在多个输出端口间负载均衡
