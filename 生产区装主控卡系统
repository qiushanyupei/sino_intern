先用串口线连接主控板（可能碰到还在uboot命令行界面的情况(特征是黄绿色箭头=>)，通过reset可以重启，退出uboot命令行）
传文件建立通过rz -b或者Winscp

改Ip命令：
sudo su
ifconfig eth1 192.168.1.200

传update，bl2（似乎是uboot版本），加执行权限，执行update文件升级，然后重启系统
改IP使能ping通，需要关闭防火墙。
打开系统软件，start_server_win.batch。IP选择本机ip，板卡型号选择V5，复制命令执行

好了后reboot重启，密码变为super/123456

2.reboot 完成后，安装小软件
sudo su
cd /app/version
chmod 777 *
./dpi_oem_dev_4755-ctc-aarch64.dfo.sh

小软件安装完成后，reboot重启

3.reboot 完成后，安装软件版本

tap_YH_DPI-1.0.26-FULL.tar.gz 上传到app/version 后解压

cd /app/version
tar -zxvf tap_YH_DPI-1.0.26-FULL.tar.gz


cd tap_YH_DPI-1.0.26-FULL
./install_mini.sh --install

装完了再reboot

5.Redis数据库安装

将redis数据库安装包及redis.conf配置文件放到主控的/home/super目录下

sudo su

rpm -ivh redis-4.0.14-6.oe2203sp1.aarch64.rpm
rm -rf /etc/redis.conf
mv /home/super/redis.conf /etc
systemctl restart redis

6.机框信息烧录与核对

手动上传firmware-cmd脚本到/home/super下
chmod +x /home/super/firmware-cmd
./firmware-cmd url://eeprom-1


搞完firmware后先给自己电脑加IP，100.任意。
然后开始格式化分区
ssh进入100.1网关输入一些命令,手动格式化

fdisk /dev/mmcblk0
n
p
1
然后可以多输几个回车，会自动填入默认值
w
mkfs.ext4 /dev/mmcblk0p1
然后reboot
通过ssh网络连接会网断，过一段时间再连接即可

xsfc放tmp文件夹，执行然后再reboot
cd /tmp
chmod 777 *
./xsfc-2.2.2_rc37-ctc-embedded-ctcA53.dfo.sh 

[root@sino-platform:/root]# fdisk /dev/mmcblk0
Device contains neither a valid DOS partition table, nor Sun, SGI, OSF or GPT disklabel
Building a new DOS disklabel. Changes will remain in memory only,
until you decide to write them. After that the previous content
won't be recoverable.


The number of cylinders for this disk is set to 119296.
There is nothing wrong with that, but this is larger than 1024,
and could in certain setups cause problems with:
1) software that runs at boot time (e.g., old versions of LILO)
2) booting and partitioning software from other OSs
   (e.g., DOS FDISK, OS/2 FDISK)

Command (m for help): n
Partition type
   p   primary partition (1-4)
   e   extended
p
Partition number (1-4): 1
First sector (16-7634943, default 16): 
Using default value 16
Last sector or +size{,K,M,G,T} (16-7634943, default 7634943): 
Using default value 7634943

Command (m for help): w
The partition table has been altered.
Calling ioctl() to re-read partition table
[root@sino-platform:/root]# mkfs.ext4 /dev/mmcblk0p1
mke2fs 1.46.5 (30-Dec-2021)
Discarding device blocks: done                            
Creating filesystem with 954366 4k blocks and 239040 inodes
Filesystem UUID: c59b3f02-5342-45e8-95f7-a574b7ff0aef
Superblock backups stored on blocks: 
        32768, 98304, 163840, 229376, 294912, 819200, 884736

Allocating group tables: done                            
Writing inode tables: done                            
Creating journal (16384 blocks): done
Writing superblocks and filesystem accounting information: done 
