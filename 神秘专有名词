现网：实际业务网
贴片：把微小的电子元器件（特别是芯片）精准地“贴”到电路板上的过程
在欣诺，通常由BASE代表SCP,(system control processor)
ffe：端口质量参数
撞件：因为外力原因使电路板上器件偏离原有位置，导致损坏
植球：用机器焊锡球在物料上，可能是外层锡球损坏，但物料本身没有问题，所以退库前要重新植球
晶圆是指制作硅半导体电路所用的硅晶片，常用来制作芯片
flow-route：控制器上配置一遍，然后就可以通过BGP下发到全网边界路由器的ACL，似乎是基于BGP协议实现的
FCS（Frame Check Sequence，帧校验），以太网帧尾部校验值。
接收端重新计算帧校验，和报文自带 FCS 不一致 → 上报 FCS 错误，代表报文在传输途中比特发生翻转、损坏。
FCS 错包本质：报文已经被弄坏了，接收侧才检出，无法直接证明 “在哪一段弄坏的”，这是核心关键点。
BGP 引流 = 通过发布 / 注入 BGP 路由，把特定流量引导到指定设备 / 链路 / 机房，比如把运营商的流量全部接引到运营商机房
NE40E：NetEngine 40E（NE40E）华为高端全业务路由器
大网路由反射器（RR，Route Reflector）：全称：BGP 路由反射器，“大网路由反射器” 是运营商 / 大型骨干网口语叫法，特指部署在大规模自治域（AS）内、用于横向分发 IBGP 路由、取消 IBGP 全网状 Full Mesh的核心 BGP 路由器（RFC4456）。
BGP路由器不用两两建立邻居关系，每个BGP路由器只需要和RR建立关系即可，减少发送消息的损耗
超核就是超级核心RR
互联网出口EBGP → 超核RR（顶层控制面，只反射路由）→ 各省二级RR → PE/骨干转发路由器（Client）
                                                                 ↓（业务数据流量）
                                                         引流 → 安全资源池（清洗DDoS、防火墙）
A3 - 亦庄 - 7750
7750：诺基亚贝尔（原阿尔卡特）7750 SR 业务路由器（Service Router），运营商大网主流高端路由器硬件平台
亦庄：机房位置（北京亦庄机房）
A3：机房机位 / 机柜 / 集群编号（运营商内部工程命名，非标准协议名词）

PE 设备（Provider Edge，运营商边缘设备），属于 MPLS‑VPN 组网里的核心设备，部署在运营商网络的边界，靠近用户侧
CXMT：ChangXin Memory Technologies长鑫内存科技
I2C Device（从机 / Slave 模式，是一种CPLD工作模式）（**I2C**：Inter‑Integrated Circuit（内部集成电路总线），device这里就是slave的意思）**CPU/COMe 是 I2C Master；XC_CPLD1、XC_CPLD2 就是 I2C‑Device（I2C 从设备）**。
在欣诺里通常是CPU是SPI主设备，CPLD是SPI从设备
弹出VPN即创建VPN  

Work灯通常是指SYS灯、RUN灯

802.11b/g/n
**802.11 是 IEEE 制定的无线局域网 (W‑WiFi) 标准，b/g/n 是三代早期 WiFi 协议，常写在一起，表示路由器 / 网卡同时兼容这三种模式**。
# 焊盘（Pad）
简单说：**PCB 上专门用来焊接元器件的铜箔区域**，锡膏、焊锡就熔在这块铜上，元件引脚 / 电极贴在这里，实现电气连通 + 机械固定。
焊盘：把元件引脚 / 引脚焊在焊盘上，把零件牢牢固定在板子上，防止晃动、脱落；焊盘底下连着 PCB 走线，焊上锡之后，元件引脚就通过焊盘接到电路走线，实现信号、电源导通
cortex是Arm厂家设计出来的 CPU 内核
SMT：Surface‑Mount Technology，表面贴装技术
Host 规则匹配:匹配HTTP报头里面的Host(域名)
提 flash：把板子上 Flash 芯片里面完整的固件、配置、数据全部读出来，保存成 bin/img 镜像文件，叫 “提 flash”
接有负载，物理链路上电源已经接上了，但是未必上电
在电路图上电路的两边**L = Live 火线(带电，对地有 220V 交流电压。摸上去会触电。)****N = Neutral 零线(变压器端接地，正常对地电压接近 0V；
但**零线不等于地线**，零线工作时有电流流过。)**
地线：PE（Protective Earth，符号⏚）黄绿双色线，保护人身安全，正常没有电流。
**NetFlow** 是思科最早提出的**流（Flow）统计技术**，属于**流级元数据**（路由器 / 交换机对**同一条五元组会话**做汇总统计，输出整条流的摘要信息，而不是输出每一个数据包。）
**Flowspec 是 BGP 的扩展（RFC8955），用 BGP 路由协议，在全网路由器之间自动下发流量匹配规则 + 执行动作，主要用来做分布式 DDoS 缓解，替代手工在每台设备敲 ACL**。
地址族：对于**<源 IP, 源端口，目的 IP, 目的端口>**这个TCP四元组的流出现时，（应该也可以是其他流量）可以分别执行多个动作
流表的基本结构是：
[哈希Key]          | [匹配掩码] | [动作(Action)] | [状态/统计]
sip + dip + sport + dport + proto      哪些位参与匹配   转发/丢弃/改包   字节数、包数、老化定时器
# LAG 口**全称：Link Aggregation Group，链路聚合组**把**多个物理以太网端口捆绑成 1 个逻辑接口**。提升带宽 + 链路冗余；单条成员链路断了，流量自动切到其他链路，业务不中断。
NHI = **Network Host Interface**，网络主机接口（交换芯片 / 业务芯片面向 CPU / 主机侧的内部接口），可以理解为设备内部网口
# testpmd行业内一般理解为：**Test Packet Forwarding Engine
PMD = **Poll Mode Driver**，轮询模式驱动（DPDK 核心网卡驱动模型）
> testpmd 直译： 报文转发测试工具**,是 DPDK（Data Plane Development Kit，数据平面开发套件）自带的**核心测试样例应用**，是网卡 / DPDK 调试最常用工具
so库：动态共享库shared object
EAL参数：**英文全称：Environment Abstraction Layer**中文：**环境抽象层
### EAL 负责的能力
- 管理大页内存（hugepage）、NUMA 内存分配（Non-Uniform Memory Access，非一致内存访问，在多CPU架构下，每个CPU都有各自的一部分内存，访问自己内存快，访问别人内存慢）
- lcore 逻辑核绑定、CPU 亲和性(硬亲和，即手动设置亲和，会强制保证所有进程都在设定的CPU上执行；linux默认是软亲和，尽量这样)
- PCI 网卡白名单 / 黑名单、PMD （poll mode driver）驱动加载
- 多进程模式（primary/secondary）、日志、定时器、原子操作、平台差异屏蔽DPDK
# LiFi（Light Fidelity，光保真）：**用光（LED 灯光）代替无线电波来无线上网**，是 WiFi 的补充技术，不是用来替代 WiFi 的。
 原理:给 LED 灯加装调制芯片，让灯**每秒几百万次高速明暗闪烁**：亮 = 1，灭 = 0。闪烁极快，人眼完全看不出灯光在闪；设备上的光电传感器捕捉光脉冲，还原成网络数据。
下行：LED 灯 → 设备；上行一般用红外传回，实现双向通信。
核心优点
1. **带宽巨大**：可见光谱是无线电频谱的上万倍，实验室速率可达百 Gbps，商用常见 1~10Gbps
2. **无电磁辐射干扰**：没有射频电波，适合**医院、飞机、核电站、石油平台**这些不能用无线电的场景
3. **安全性高**：光穿不透墙壁，房间外面接收不到信号，不容易被远程窃听
缺点
1. **必须视线可见**：物体挡住光源就断网；强光、阳光会干扰信号
2. **终端需要专用光接收传感器**：现在手机电脑原生不带 LiFi 接收器，要加适配器
3. **移动性差**：人走动遮挡灯光就容易掉线
典型用途
- 飞机、医院、手术室（禁止射频干扰设备）
- 涉密机房、银行（防止无线信号泄露）
- 工业车间、油气工厂（避免射频引爆风险）
- 车联网：车灯之间互相通信（V2V）
- 高密度办公空间，分担 WiFi 压力
# RSS英文全称：Receive Side Scaling，中文：接收端扩展，俗称网卡 RSS 多队列
是**网卡硬件**实现的流量分流技术：
1. 网卡内置多个硬件rx queue；
2. 网卡硬件提取报文**四元组 / 五元组**（源 IP、目的 IP、源端口、目的端口，可选协议号）做 Toeplitz 哈希（RSS常用，存储占用空间小）；
3. 通过哈希 + 间接表（indirection table）（哈希映射查表数组），把**同一流的所有报文固定分到同一个 RX 队列，不同流分到不同队列**；
4. 每个 RX 队列绑定独立中断（中断来源于网卡收到数据包）（硬件通过中断线发送信号给CPU，），不同中断调度到不同 CPU 核心处理，**把网络软中断（收到硬件中断后真正需要处理的内核任务）压力分散到多核**，解决单核心处理网络包瓶颈
round-robin这种负载均衡方式：**逐包**，包依次轮换端口，带宽利用率高，但存在报文乱序风险
GA WA是公安WA = Wireless Access 无线接入
固定位置特征码规则：距离报文开始有多少偏移量，根据正则或字符串等内容进行匹配
全包浮动位置特征码规则：报文中只要出现
复合规则：一条规则同时包含多个判定条件，逻辑组合（与、或）
规则不扩展：部分有掩码的规则，有的设备可能根据掩码生成掩码之间的笛卡尔积，需要存储很多项，这样匹配快，但是占用存储空间
inner‑sdip‑sdp‑p = inner sip + inner dip + sport 源端口 + dport 目的端口 + protocol 协议号（区分不同网络协议）
插损：**插入该器件 / 端口链路前后，输出信号功率相比输入信号功率的衰减量**，单位 **dB**高插损可以通过更大的FFE参数来弥补插损
ffe参数中cn3、cn2等标识中n代表负号，n3即-3，也就是n3是指第-3个n3
hal_mt_nb（硬件抽象层_Marvell芯片牌子_nb是具体PHY编号）
SIP：**Session Initiation Protocol****会话初始协议**，是 **应用层信令协议**，3GPP IMS/VoLTE 体系的核心控制协议，用来**建立、修改、终止多媒体会话**。
MW接口：3GPP（Third Generation Partnership Project**，全球通讯组织指定的一套规范）的一种特殊接口编号，是IMS 域内**P-CSCF ↔ I-CSCF ↔ S-CSCF**之间的接口（CSCF 全称：Call Session Control Function，呼叫会话控制功能，p指代理，一般是接入的入口,I指Interrogating查询，查路由，进行转发；s指服务，主要处理业务逻辑）
网元：逻辑单位，能独立完成一类网络功能、可以被网管单独管理
网管：**逻辑上是一套管理平台**。网管管理多个网元
RCS：**Rich Communication Suite（也叫 Rich Communication Services），中文：富媒体通信套件 / 富通信服务，国内运营商商用名称叫「5G 消息」**。
